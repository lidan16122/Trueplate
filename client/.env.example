# Copy to client/.env. VITE_* values are included in the public browser bundle.
# Backend credentials, including Google credentials, belong in server/.env.

# Keep empty locally and in production: /api requests use the frontend's origin.
# Vite proxies them to localhost:8000; the deployed Worker uses its API_ORIGIN secret.
VITE_API_BASE_URL=
